using System;
using UnityEngine;

[Serializable]
public class {0}StateMachine_{1} : IStateMachine
{{
    public void OnStateEnter(AnimationData state)
    {{
        Debug.Log("OnStateEnter");
    }}
    public void OnStateUpdate(AnimationData state)
    {{
        Debug.Log("OnStateUpdate");
    }}

    public void OnStateExit(AnimationData state)
    {{
        Debug.Log("OnStateExit");
    }}
}}

[CreateAssetMenu(fileName = "{1}", menuName = "StateMachines/{0}/{1}")]
public class {0}StateMachineScript_{1} : StateMachineScript
{{
    public override IStateMachine StateMachine
    {{
        get
        {{
            if (stateMachine == null) stateMachine = new {0}StateMachine_{1}();
            return stateMachine;
        }}
    }}
}}